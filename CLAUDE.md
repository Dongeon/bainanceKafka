# 협업 규칙

## CRITICAL: Git 커밋/푸시 금지
사용자가 명시적으로 "커밋해줘" / "푸시해줘" 라고 말하기 전에는 `git commit` / `git push` 를 절대 실행하지 않는다.
코드 수정 완료 후에도 커밋을 제안하지 않는다. 사용자가 먼저 요청할 때만 진행.

## Java 코드 스타일
람다 표현식(`() ->`, `x ->`) 사용 금지. 반드시 익명 클래스로 대체.

## 인프라 상태 확인 금지
Kafka, MariaDB는 항상 실행 중으로 간주하고 상태 확인 없이 바로 작업 진행.
